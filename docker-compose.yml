version: '3.8'

services:
  algovizard:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONPATH=/app
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped